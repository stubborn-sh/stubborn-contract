<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xmlns="http://maven.apache.org/POM/4.0.0"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>sh.stubborn</groupId>
		<artifactId>stubborn-parent</artifactId>
		<version>0.1.0-SNAPSHOT</version>
		<relativePath>..</relativePath>
	</parent>

	<artifactId>stubborn-migration</artifactId>
	<packaging>jar</packaging>
	<name>Stubborn Contract Migration</name>
	<description>OpenRewrite recipes to migrate from Spring Cloud Contract to Stubborn Contract</description>

	<properties>
		<openrewrite.version>8.87.3</openrewrite.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.openrewrite</groupId>
			<artifactId>rewrite-java</artifactId>
			<version>${openrewrite.version}</version>
		</dependency>
		<dependency>
			<groupId>org.openrewrite</groupId>
			<artifactId>rewrite-maven</artifactId>
			<version>${openrewrite.version}</version>
		</dependency>
		<!--
		  NOTE: rewrite-gradle is intentionally NOT a dependency. The migrate-from-scc recipe
		  operates only on Maven POMs (org.openrewrite.maven.*) and Java sources
		  (org.openrewrite.java.*); it contains no Gradle (org.openrewrite.gradle.*) recipes.
		  rewrite-gradle would drag in rewrite-kotlin and the entire Kotlin runtime — which at
		  the versions OpenRewrite resolves is exposed to CVE-2026-53914 (CVSS 9.8) — for zero
		  functional benefit. If/when Gradle build-script migration recipes are added, re-add
		  rewrite-gradle and pin the Kotlin runtime to a fixed (>= 2.4.20 GA) version at that time.
		-->
		<!-- test -->
		<dependency>
			<groupId>org.openrewrite</groupId>
			<artifactId>rewrite-test</artifactId>
			<version>${openrewrite.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.openrewrite</groupId>
			<artifactId>rewrite-java-17</artifactId>
			<version>${openrewrite.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.openrewrite</groupId>
			<artifactId>rewrite-java-21</artifactId>
			<version>${openrewrite.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-api</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
