<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xmlns="http://maven.apache.org/POM/4.0.0"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>sh.stubborn</groupId>
		<artifactId>stubborn-contract-docker-parent</artifactId>
		<version>0.1.3-SNAPSHOT</version>
		<relativePath>..</relativePath>
	</parent>

	<artifactId>stubborn-contract-stub-runner-docker</artifactId>
	<packaging>pom</packaging>
	<version>0.1.3-SNAPSHOT</version>
	<name>Stubborn Contract Stub Runner Docker</name>
	<description>Stubborn Contract Stub Runner Docker</description>

	<dependencies>
		<dependency>
			<groupId>sh.stubborn</groupId>
			<artifactId>stubborn-contract-stub-runner-app</artifactId>
			<!-- Not managed by the BOM (repackaged executable, excluded from publishing); pin the reactor version. -->
			<version>${project.version}</version>
			<scope>compile</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<failIfNoTests>false</failIfNoTests>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<id>copy-dependencies</id>
						<phase>package</phase>
						<goals>
							<goal>copy</goal>
						</goals>
						<configuration>
							<artifactItems>
								<artifactItem>
									<groupId>${project.groupId}</groupId>
									<artifactId>stubborn-contract-stub-runner-app</artifactId>
									<version>${project.version}</version>
									<type>jar</type>
									<!-- The 'exec' classifier is the Spring Boot repackaged, executable
									     fat jar. The main (no-classifier) artifact is the thin library
									     jar with no Main-Class, which cannot be `java -jar`-run. -->
									<classifier>exec</classifier>
									<overWrite>true</overWrite>
									<outputDirectory>${project.build.directory}/libs
									</outputDirectory>
									<destFileName>stub-runner-boot.jar</destFileName>
								</artifactItem>
							</artifactItems>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>com.spotify</groupId>
				<artifactId>dockerfile-maven-plugin</artifactId>
				<version>1.4.13</version>
				<executions>
					<execution>
						<id>default</id>
						<goals>
							<goal>build</goal>
							<goal>push</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<repository>
						${docker.registry.organization}/stubborn-contract-stub-runner
					</repository>
					<tag>${project.version}</tag>
					<username>${env.DOCKER_HUB_USERNAME}</username>
					<password>${env.DOCKER_HUB_PASSWORD}</password>
				</configuration>
				<dependencies>
					<dependency>
						<groupId>org.codehaus.plexus</groupId>
						<artifactId>plexus-archiver</artifactId>
						<version>4.9.1</version>
					</dependency>
				</dependencies>
			</plugin>
		</plugins>
	</build>
</project>
